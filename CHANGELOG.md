# 更新日志

## [0.1.0] - 2026-09-16

首个公开版本。

### 功能

- 悬浮气泡：可拖动、位置记忆、有收藏时显示数量角标
- 本地菜库 4,423 道，离线挑菜，不消耗 token
- 一次三道：前两道必定是主流菜，第三道按权重随机；保底道数可配
- 筛选条件：做法、菜系、城市、口味、自己做 / 点外卖、主流保底、收藏加权
- 餐次：按本地钟点自动判断早餐 / 午餐 / 晚餐 / 夜宵，也可手动指定或选不限
- 收藏：写入本地文件，有独立面板可查看与逐条取消
- 忌口：写入本地文件，列成可单条删除的小标签
- AI 按钮：写做法、估热量、大肥鱼评价（关闭思考、限制字数以控制开销）
- 气泡图标：随包自带的透明底 PNG

### 数据

- `data/dishes.json` 4,423 道，字段为 name / cuisine / foreign / scene / star / weight / methods / meals
- 主流菜 132 道，做法标签覆盖 2,168 道，餐次标签覆盖 2,344 道
- 抓取阶段留下的 12 条重复菜已清掉：成对克隆（`爆肚(一)` / `爆肚(二)` 这类同菜同菜系同权重）只留一条，
  `醋溜白菜(二)`、`白切羊肉(一)` 这类与库内正牌条目重复的整条删除，错字残留的 `螺狮粉(干重，均值)` 也一并移除
- 10 条只有一份、后缀纯属噪音的菜（`脆皮鸭(一)`、`软熘豆腐(一)` 等）去掉 `(一)`/`(二)` 后缀保留，不连菜一起删
- 带语义的括号变体（`饺子(三鲜馅)`、`煎饼果子(加薄脆)`、`粽子(肉)` 等）是不同口味/做法的独立条目，保持不动

### 接口

- `GET /dsh-meal/meta`、`GET /dsh-meal/recommend`、`POST /dsh-meal/ask`
- `GET|POST /dsh-meal/favorites`、`GET|POST /dsh-meal/prefs`
- `POST /dsh-meal/recent`、`GET /dsh-meal/icon.png`

### 工具

- `tools/dedupe_dishes.py`：清重复条目，默认预演、`--write` 才落盘，可重复运行

### 兼容性

- DSH Desktop 0.8.x（宿主 `@deepseek-ai/dsh` 0.1.2-rc.x 与 Cordis 4.x）
- Node.js ≥ 20，零运行时依赖，无构建步骤
- `@deepseek-ai/*` 的 peer 范围带显式预发布分支：`*` 在 semver 里匹配不到任何 `-rc.` 构建
  （实测 `satisfies("0.1.2-rc.1", "*") === false`），而宿主跑的正是 `dsh-host-webserver@0.1.2-rc.1`，
  所以逐段写出了范围。插件没有 `import` 这两个包，只用宿主注入的服务，属声明层面的修正。

